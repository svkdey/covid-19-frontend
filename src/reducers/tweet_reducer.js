// import { CONFIRMED_CASE } from "../actions/types.js";
const initialTweets = {};
export default function(state = initialTweets, action) {
  switch (action.type) {
    default:
      return state;
  }
}
